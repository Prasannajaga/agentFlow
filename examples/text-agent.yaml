name: text-agent
version: 1
description: Simple research agent

provider:
  type: fake
  model: stub-model

system_prompt: |
  You are a focused research agent.

runtime:
  max_steps: 12
  timeout_seconds: 900
  retry:
    max_attempts: 3
    backoff_seconds: 5

tools: []

task:
  type: research
  input_schema:
    topic: string
    depth: string

telemetry:
  trace_enabled: true
  log_level: info
  store_prompts: true

outputs:
  save_final_text: true
  save_artifacts: true
